# ============================================
# 서버 설정
# ============================================

# 실행 환경 (development | production | test)
# 기본값: development
NODE_ENV=development

# 서버 포트
# 기본값: 3000
# Railway에서는 자동으로 설정되므로 변경 불필요
PORT=3000

# ============================================
# CORS 설정
# ============================================

# 허용된 도메인 목록 (쉼표로 구분)
# 개발 환경: 여러 도메인 허용 가능
# 프로덕션: 반드시 설정 필요
# 예시: ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
# 예시: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ============================================
# Rate Limiting 설정 (선택사항)
# ============================================

# Rate Limiting 시간 윈도우 (밀리초)
# 기본값: 900000 (15분)
# RATE_LIMIT_WINDOW_MS=900000

# Rate Limiting 최대 요청 수
# 기본값: 100
# RATE_LIMIT_MAX=100
