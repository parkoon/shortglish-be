# ============================================
# 서버 설정
# ============================================

# 실행 환경 (development | production | test)
# 기본값: development
NODE_ENV=development

# 서버 포트
# 기본값: 4000
# Railway에서는 자동으로 설정되므로 변경 불필요
PORT=4000

# ============================================
# CORS 설정
# ============================================

# 허용된 도메인 목록 (쉼표로 구분)
# 개발 환경: 여러 도메인 허용 가능
# 프로덕션: 반드시 설정 필요
# 예시: ALLOWED_ORIGINS=http://localhost:4000,http://localhost:5173
# 예시: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=http://localhost:4000,http://localhost:5173

# ============================================
# Rate Limiting 설정 (선택사항)
# ============================================

# Rate Limiting 시간 윈도우 (밀리초)
# 기본값: 900000 (15분)
# RATE_LIMIT_WINDOW_MS=900000

# Rate Limiting 최대 요청 수
# 기본값: 100
# RATE_LIMIT_MAX=100

# ============================================
# 토스 로그인 설정
# ============================================

# 토스 API 기본 URL
# 기본값: https://apps-in-toss-api.toss.im
# TOSS_API_BASE_URL=https://apps-in-toss-api.toss.im

# 복호화 키 (Base64 인코딩)
# 토스 콘솔에서 이메일로 받은 복호화 키를 입력하세요
# TOSS_DECRYPTION_KEY=your_base64_encoded_decryption_key

# AAD (Additional Authenticated Data)
# 기본값: TOSS
# TOSS_AAD=TOSS

# 콜백 Basic Auth 설정 (선택사항)
# 토스 콘솔에서 콜백 URL과 함께 설정한 Basic Auth 정보
# TOSS_CALLBACK_BASIC_AUTH_USERNAME=your_username
# TOSS_CALLBACK_BASIC_AUTH_PASSWORD=your_password
